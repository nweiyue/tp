package atas.ui.sessionlist.session;

/**
 * Represents a {@code Presence} label representing either of the states: present or absent.
 */
public class PresenceLabel extends AttributesLabel {

    /**
     * Creates a {@code ParticipationLabel} with the given text to display.
     */
    public PresenceLabel(String text, boolean isPositive) {
        super(text, isPositive);
    }

}
